package com.example.facebook.repositories;

import com.example.facebook.entities.FotosMegusta;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FotosMegustaRepository extends JpaRepository<FotosMegusta, Integer> {
}
